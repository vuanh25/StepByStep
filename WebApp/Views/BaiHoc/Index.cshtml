@model IEnumerable<WebApp.Models.Entities.BaiHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<body>
    <div class="row">
        <div class="col-3 ">
            <span class="list-group-item p-1 p-lg-1 bg-danger text-light">C# cở bản</span>
            @foreach (var item in Model)
            {

                <a class="list-group-item p-1 p-lg-1 list-group-item-action list-group-item-warning" href="@Url.Action("Index", "BaiHoc",new { @id = item.IdBaiHoc,@strURL = Request.Url.ToString()})">
                    @Html.DisplayFor(modelItem => item.TenBaiHoc)
                </a>


            }
        </div>
        <div class="col-9">
            @foreach (var item in Model)
            {
                if (item.IdBaiHoc == @ViewBag.IdbaiHoc)
                {
            <a class="list-group-item p-1 p-lg-1 list-group-item-action list-group-item-warning">
                @Html.DisplayFor(modelItem => item.NoiDung1)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung2)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung3)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung4)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung5)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung6)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung7)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung8)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung9)
                <pre></pre>
                @Html.DisplayFor(modelItem => item.NoiDung10)
            </a>
                }
            }

        </div>
    </div>
</body>
